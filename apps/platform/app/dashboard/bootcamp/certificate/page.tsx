import { Card } from '@devscale/shared-ui';

export default async function Page() {
  return (
    <main className="space-y-8">
      <Card className="w-full text-center">No available certificate yet</Card>
    </main>
  );
}
